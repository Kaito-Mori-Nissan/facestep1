on:
  workflow_call:
    inputs:
      matrix:
        required: true
        type: string

jobs:
  Bug-Finder:
    runs-on: self-hosted
    strategy:
      matrix: ${{ fromJson(inputs.matrix) }}
    steps:
      - run: echo "done."