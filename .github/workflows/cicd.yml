name: cicd pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: |
          ls -l ./src/shell/sample1.sh
          ls -l ./src/shell/sample2.sh
      - name: run script
        run: bash ./src/shell/sample1.sh